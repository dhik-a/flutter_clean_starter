name: data_profile
description: "Data module for user profile."

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  common:
    path: ../../common
  domain_profile:
    path: ../../domain/domain_profile

  cupertino_icons: ^1.0.8
  get_it: ^8.0.0
  injectable: ^2.5.0
  freezed_annotation: ^2.4.4

dev_dependencies:
  build_runner: ^2.4.14
  flutter_test:
    sdk: flutter
  freezed: ^2.5.7
  injectable_generator: ^2.6.2
